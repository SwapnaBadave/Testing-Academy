console.log(API_END_APP_VWO_COM);
console.log("dsadsa");
if (true) {
}
    //let API_END_APP_VWO_COM = "https://app.vwo.com/api";// let is block scope variable. it is not hoisted. it is not accessible before declaration. 
    // it will give error if we try to access it before declaration. 
    // it will give error if we try to redeclare it. it will give error if we try to access it outside the block in which it is declared.
const API_END_APP_VWO_COM = "https://app.vwo.com/api";// const is block scope variable. it is not hoisted. it is not accessible before declaration. 
    // it will give error if we try to access it before declaration. 
    // it will give error if we try to redeclare it. it will give error if we try to access it outside the block in which it is declared.
