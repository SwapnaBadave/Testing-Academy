const baseUrl = "https://api.example.com/data";
const baseUrl = "https://api.example.com/data";
// The value of baseUrl cannot be changed, but we can still modify the contents of the object it points to if it were an object.
// For example:
// baseUrl = "https://api.anotherexample.com/data"; // This will throw an error because we cannot reassign a const variable.